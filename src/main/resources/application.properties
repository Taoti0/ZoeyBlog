# thymeleaf config
spring.thymeleaf.cache=false

# datasource config
spring.datasource.url=jdbc:mysql://localhost:3306/zoey_blog_db?useUnicode=true&characterEncoding=utf8&autoReconnect=true&useSSL=false&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=hedixuan..5253979

# mybatis config
# 存放 mapper 文件的文件夹在 resource 文件夹下，和 application.properties 同级
mybatis.mapper-locations=classpath:mapper/*Mapper.xml